services:
  nous-hermes-runtime:
    build: ./nous-hermes-runtime
    container_name: vectraclaw-nous-hermes
    restart: unless-stopped
    volumes:
      - nous-hermes-config:/root/.hermes
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9120/health"]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 180s

  backend:
    build: .
    container_name: vectraclaw-backend
    restart: unless-stopped
    ports:
      - "3100:3000"
    env_file:
      - .env
    environment:
      PORT: "3000"
      NOUS_HERMES_RUNTIME_URL: http://nous-hermes-runtime:9120
    depends_on:
      # service_started (NÃO _healthy): backend não bloqueia esperando o
      # nous-hermes ficar healthy. O healthcheck dele tem start_period 180s +
      # boot lento do runtime → recreate do backend levava 10min-1h. Nous-hermes
      # é provider fallback; backend tolera ele aquecendo e sobe em segundos.
      nous-hermes-runtime:
        condition: service_started
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  daemon:
    build: .
    container_name: vectraclaw-daemon
    restart: unless-stopped
    command: ["python", "-m", "src.agent_daemon"]
    env_file:
      - .env
    environment:
      NOUS_HERMES_RUNTIME_URL: http://nous-hermes-runtime:9120
    depends_on:
      backend:
        condition: service_healthy
      nous-hermes-runtime:
        condition: service_started

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: vectraclaw-tunnel
    restart: unless-stopped
    command: tunnel --config /etc/cloudflared/config.yml run
    volumes:
      - ./cloudflared:/etc/cloudflared:ro
    depends_on:
      backend:
        condition: service_healthy

volumes:
  nous-hermes-config:
